[project]
name = "md-reader"
version = "0.1.1"
description = "Windows Markdown reader"
requires-python = ">=3.10"
dependencies = [
  "Markdown>=3.6",
  "Pygments>=2.17",
  "PySide6>=6.7",
]

[project.scripts]
md-reader = "md_reader.app:main"

[tool.setuptools.packages.find]
where = ["src"]

